
// we need have different level which approves the incoming amount request with some eligibiliy
// like manager (cost< 10000 && )
// like srmanager (cost > 10000 && cost < 50000)
// like director (cost > 100000 && cost < 50000000)
// like if ceo (cost > 100000000)

// Main Approver Class which will handlerapprove();
//

